"""Video-bot content layer.

Everything in this package produces the *scene spec* — the declarative seam
described in docs/UPGRADE-PLAN.md §2. Nothing here renders pixels; the motion
layer consumes the spec and is deliberately swappable.
"""

__version__ = "0.1.0"
